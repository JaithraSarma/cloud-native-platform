apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: azure-kv-secrets
spec:
  parameters:
    keyvaultName: "kv-cloudplatform-dev"
    tenantId: "REPLACE_WITH_YOUR_TENANT_ID" # This should be set via CI/CD or script
